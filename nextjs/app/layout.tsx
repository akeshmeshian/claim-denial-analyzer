import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Claim Denial Analyzer — Understand Your Insurance Denial",
  description:
    "Upload your denial letter and insurance policy. Our AI analyzes coverage arguments, policy language, and exclusions so you know exactly where you stand.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        {children}
      </body>
    </html>
  );
}
